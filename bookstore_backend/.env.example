# Security
SECRET_KEY=CHANGE_ME_DEV_SECRET
ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_ALGORITHM=HS256

# Database (defaults to local SQLite if not set)
DATABASE_URL=sqlite:///./bookstore.db

# CORS Allowed Origins (comma-separated or use * for all)
ALLOW_ORIGINS=*
