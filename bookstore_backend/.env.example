# Environment variables for the Bookstore Backend

# SQLAlchemy database URL (defaults to sqlite:///./bookstore.db if not provided)
# Examples:
#   SQLite file (default): sqlite:///./bookstore.db
#   SQLite memory (dev only): sqlite+pysqlite:///:memory:
#   PostgreSQL: postgresql+psycopg2://user:password@localhost:5432/bookstore
#   MySQL: mysql+pymysql://user:password@localhost:3306/bookstore
DATABASE_URL=sqlite:///./bookstore.db
